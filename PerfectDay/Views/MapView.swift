import SwiftUI

struct MapView: View {
    var body: some View {
        Text("Map")
            .font(.largeTitle)
            .padding()
    }
}

struct MapView_Previews: PreviewProvider {
    static var previews: some View {
        MapView()
    }
}

#Preview {
    MapView()
}
